package secondcourse

object test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(58); 
  val a = List(10,2,3);System.out.println("""a  : List[Int] = """ + $show(a ));$skip(25); 
 val tree = fromList(ls);System.out.println("""tree  : <error> = """ + $show(tree ))}
}
